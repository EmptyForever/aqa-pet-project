# Строки, числа и булевы значения

base_url = "https://jsonplaceholder.typicode.com"  # строка
user_id = 1                                         # число
is_active = True                                    # булево

print(f"Базовый URL: {base_url}")
print(f"ID пользователя: {user_id}, тип: {type(user_id)}")
print(f"Активен: {is_active}, тип: {type(is_active)}")